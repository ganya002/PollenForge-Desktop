[project]
name = "nexum-backend"
version = "1.2.1"
description = "Nexum local tools backend"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.29",
  "httpx>=0.27",
  "websockets>=12",
]

[tool.uv]
package = false
