# Contributing / agent instructions

Any human or AI working in this repo: read **[AGENTS.md](AGENTS.md)** first.

That document covers versioning, GitHub Releases (Mac + Windows installers), and in-app updates. Install and “blocked by Windows/macOS” steps are in **[docs/INSTALL.md](docs/INSTALL.md)**.

Do not keep a second, Cursor-only copy of that process. `.cursor/rules` only points here.
